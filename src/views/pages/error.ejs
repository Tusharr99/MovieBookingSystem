<%- include('../partials/header', { title }) %>
<section class="text-center space-y-4">
  <p class="text-sm uppercase tracking-wide text-rose-300">Error</p>
  <h1 class="text-3xl font-semibold"><%= status || 500 %></h1>
  <p class="text-slate-300"><%= message || 'Something went wrong' %></p>
  <a href="/" class="inline-flex px-4 py-2 rounded bg-emerald-500 text-slate-950 font-semibold">Back to home</a>
</section>
<%- include('../partials/footer') %>

